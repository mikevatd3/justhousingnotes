[project]
name = "justhousingnotes"
version = "0.1.0"
description = "Project including GPT2 experiemnts and compiling notes on interpretability into a Quarto notebook"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "plotly>=6.8.0",
    "sae-lens>=6.44.4",
    "transformer-lens>=3.5.1",
]
